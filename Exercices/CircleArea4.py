# 4. Write a Python program which accepts the radius of a circle from the user and compute the area.
import math
radius = float(input('Give a measure\n'))
area = math.pi*radius*radius

print(f'The area is {area}')


